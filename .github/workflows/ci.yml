name: Continuous integration

on:
  push:
  pull_request:

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: hello world
        run: |
          echo "${{ event.pull_request.head.ref }}"
          echo "${{ event.ref }}"
